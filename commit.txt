git add . && git commit -m "fix(pipeline): optimized RAM/Disk usage and improved logging" -m "Resolved critical resource exhaustion issues during bulk loading:" -m "1. Disk (Errno 28): Implemented auto-cleanup in pipeline.py and added SEPA_TEMP_DIR config to offload temp files from /tmp to local disk." -m "2. RAM (OOM): Optimized query_table.py to use column pruning (selected_fields) instead of full table scans." -m "3. Observability: Added [POSTGRES] and [ICEBERG] log prefixes to loader.py."
